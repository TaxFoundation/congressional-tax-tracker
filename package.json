{
  "name": "congressional-tax-tracker",
  "version": "1.0.0",
  "description": "Interactive HTML table of congressional tax plans.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-data": "^1.2.1",
    "gulp-pug": "^3.0.3",
    "gulp-sass": "^2.1.1",
    "gulp-swig": "^0.8.0",
    "gulp-webserver": "^0.9.1",
    "mkdirp": "^0.5.1",
    "pug": "^2.0.0-beta3",
    "rimraf": "^2.5.2"
  },
  "scripts": {
    "prebuild": "rimraf dist && mkdirp dist/css && mkdirp dist/js && mkdirp dist/images",
    "start": "gulp build"
  }
}
